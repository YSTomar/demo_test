<%= form_for(@leacture) do |f| %>
  <% if @leacture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leacture.errors.count, "error") %> prohibited this leacture from being saved:</h2>

      <ul>
      <% @leacture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :presentation_id %><br>
    <%= f.number_field :presentation_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
