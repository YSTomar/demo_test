<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @presentation.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @presentation.description %>
</p>

<p>
  <strong>PPT:</strong>
  <% debugger %>
  <a href=<%= "#{request.protocol}#{request.host_with_port}#{@presentation.document.url}" %> class="embed">PPT view</a>
</p>

<%= link_to 'Edit', edit_presentation_path(@presentation) %> |
<%= link_to 'Back', presentations_path %>

<script>
$(document).ready(function() {
	$('a.embed').gdocsViewer({width: 600, height: 750});
});
</script>
